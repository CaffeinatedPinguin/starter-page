/**
 * Storage keys used by both deployment forms. The addon stores config and
 * preferences under these keys in extension storage; web preferences use the
 * same names in localStorage.
 */
export const ADDON_CONFIG_KEY = 'starter-page-config';
export const PREFERENCES_KEY = 'starter-page-preferences';